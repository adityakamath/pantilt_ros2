[tool.ruff]
line-length = 99
target-version = "py312"

[tool.ruff.lint]
# No import sorting (I): ament_flake8 owns import order
select = ["E", "F", "W"]
ignore = [
    "E501",  # line length - not enforced here; several pre-existing lines already exceed it
]
